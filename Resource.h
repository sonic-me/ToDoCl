#pragma once

// Icon resource (optional - place TaskTicket.ico next to the .rc file and
// uncomment the ICON line in TaskTicket.rc to use it).
#define IDI_APPICON     101

// The manifest resource id that declares Per-Monitor-V2 DPI awareness.
// (Referenced directly by numeric id 1 / RT_MANIFEST in the .rc file.)
